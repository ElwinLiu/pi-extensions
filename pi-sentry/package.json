{
  "name": "pi-sentry",
  "version": "1.0.0",
  "description": "Permission/impact gate extension with execute prompt",
  "main": "index.ts",
  "scripts": {
    "test:tool-assessment": "npx --yes tsx --test test/tool-assessment.test.ts"
  },
  "pi": {
    "extensions": ["./index.ts"]
  }
}
